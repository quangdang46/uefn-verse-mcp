## https://dev.epicgames.com/documentation/en-us/fortnite/verse-api/versedotorg/verse/fitsinplayermap

# FitsInPlayerMap function

Learn technical details about the FitsInPlayerMap function.

|  |  |
| --- | --- |
| Verse `using` statement | `using { /Verse.org/Verse }` |

`FitsInPlayerMap(Value:t where t:):t`

## Parameters

`FitsInPlayerMap` takes the following parameters:

| Name | Type | Description |
| --- | --- | --- |
| `Value` | `t` |  |
| `t` |  |  |

## Attributes, Specifiers, and Effects

### Specifiers

The following specifiers determine how you can interact with `FitsInPlayerMap` in your programs. For the complete list of specifiers, see the [Specifiers Page](https://dev.epicgames.com/documentation/fortnite/specifiers-and-attributes-in-verse).
