# Agent memory

## 2026-05-08

- **MCP anti-crash throttle:** **`mcp_bridge.py`** — default **`TICK_BATCH_LIMIT=1`** (was 5 commands/frame → stall/crash). **`MAX_QUEUE_DEPTH`** + HTTP **503** when saturated; **`get_status`** exposes queue/tick/http wait; env **`UEFN_MCP_TICK_BATCH_LIMIT`**, **`UEFN_MCP_MAX_QUEUE_DEPTH`**, **`UEFN_MCP_HTTP_WAIT_SEC`**. **`server/bridge.py`** — **`RLock`** serializes outbound HTTP + optional **`UEFN_MCP_CLIENT_COOLDOWN_SEC`**; **`HTTPError`** parsing for editor errors.
- **Toolbelt audit:** Added **`scripts/audit_uefn_tools.py`** — `py_compile` all tool modules, require **`**kwargs`** on every **`@register_tool`** handler, detect duplicate tool names. Fixed **`core_safety_audit`** in **`core/safety_gate.py`** (missing **`**kwargs`**). Synced **`uefn_tools.__tool_count__`** → **363**, **`__category_count__`** → **54**; docs/MCP strings no longer hardcode **358**.
- **Terrain MCP tools:** Repo **`landscape_tools.py`** includes **`terrain_blockout_*`**, **`terrain_noise_selected`** (folder query), **`terrain_flatten`**, **`terrain_ridge_line`**, **`terrain_path_cut`**. After deploy, **`importlib.reload(landscape_tools)`** may be needed before **`describe_tool`** sees new registrations; **`list_tools`** category **Landscape** lists **8** tools when synced.

## 2026-05-07

- **LICENSE + README:** Added root **`LICENSE`** — combined terms: AGPL-3.0 + UEFN Toolbelt visible-attribution addendum (Ocean Bennett / UEFN-TOOLBELT); MIT block for KirChuvakov upstream (uefn-mcp-server / uefn-verse-server); note on AGPL applying to combined distribution. README **Origins and Credits** + **`### License`** — fork guidance, required Toolbelt credit line, pointer to full AGPL URL.
- **LICENSE wording:** Clarified Toolbelt “equivalent credit” line to link explicitly to **UEFN-TOOLBELT** repo (avoid “this repository” = fork ambiguity). User asked VN: compliance with both upstream authors — yes if README keeps visible Toolbelt credit + **`LICENSE`** stays with Ocean & KirChuvakov notices; AGPL full text via gnu.org link (same style as upstream Toolbelt notice).

## 2026-05-06

- **Landscape design help:** UEFN **`landscape_tools`**: **`landscape_list`** / **`landscape_audit`** / assign material — **no** Python sculpt/new Landscape (**see `landscape_tools.py` docstring**). After **`Landscape`** placement in-editor, MCP can audit; user **`TestMCP`** showed **`count: 0`** post-clean.
- **Landscape custom material (MCP + tool):** **`execute_python`** created **`/Game/Materials/MI_CustomLandscape.MI_CustomLandscape`** — MIC parented to **`M_Athena_Terrain_TropicalBiome_Inst_Basic`** (Athena Landscape shader chain). Duplicate Epic **`M_Athena_Terrain_DefaultBiome_02`** via AssetTools timed out; MIC-parent pattern works. Repo: **`landscape_material_create_project_mic`** in **`landscape_tools.py`** — assign on Landscape via **`landscape_set_material`** when terrain exists.
- **MCP terrain workaround:** **`spawn_actor_from_class(Landscape)`** yields **`LandscapePlaceholder`** only — not editable terrain. **`execute_python`** placed **`MCP_GroundPlane`** (**`/Engine/BasicShapes/Plane`**, scale ~80×80×1 ≈ **80 m** square floor); **`landscape_list`** stays **0** until user adds Landscape in **Landscape Mode**.
- **MCP map clean-all:** **`execute_python`** destroyed **88** non-essential actors (all **`Cube*`** meshes, **`GridPlane*`**, **`LevelBounds`**, etc.); kept **`IslandSettings0`**, **`GlobalPostProcess`**, **`WorldDataLayers`**, **`WorldPartitionMiniMap`**. Removing **`LevelBounds`** may cascade removals on some builds — respawned **2×** **`FortPlayerStartCreative`** (**`PlayerSpawn_A/B`** @ ±245,0,116). No directional/skylight actors remained after clean (none listed pre-clean in retained set).
- **MCP map clean:** **`delete_actors`** removed **Teleporter**, **Teleporter2**, **Device Floor VehicleSpawner C**, **Button**, **Button2**; **`execute_python`** **`destroy_actor`** removed **20×** **`MCP_Tree_*`**; kept **IslandSettings** / spawns / grid; **`save_current_level`** OK.
- **MCP spawn Button + verify:** **`spawn_actor`** Creative Button → **`Button2`** @ **[1200,-227,200]**; **`verse_list_devices`** (`name_filter` **Button**) reports **2×** **`BP_Creative_Button_C`** (**`Button`**, **`Button2`**); **`save_current_level`** OK.
- **MCP Button inspect:** `verse_list_devices` → **`verse_export_report`** (`verse_device_report.json`; **`BP_Creative_Button_C`** may show **`properties: {}`**); **`get_actor_properties`** often sparse. **`execute_python`** component inventory surfaces **`FortGameplayTriggerMessageComponent`** / **`FortGameplayReceiverMessageComponent`** / **`FortGameplayMessageDispatcherComponent`** (Creative channel plumbing); gameplay channel ints usually live on designer **`UserKnob`** rows in **Details**, not `getattr` on actor root.
- **Button device details (inspect):** Editor **Details** on selected Button; **`verse_list_devices`** / **`verse_select_by_name`** / **`verse_export_report`** → **`Saved/uefn_tools/verse_device_report.json`** (`properties` via schema + **`_get_actor_properties`** fallback). MCP: **`get_actor_properties`** / **`get_selected_actors`** by Outliner label. User prefers **editor channel wiring** over Verse for Button→Campaign control.
- **UEFN MCP trees (batch):** **`execute_python`** + **`EditorLevelLibrary.spawn_actor_from_class`** placed **20** Athena environment trees (rotated medium/pine mix) in a **5×4** grid (**450 cm** spacing), centered on viewport XY (**~5574, 1006**), **`floor_z=116`** aligned with **`FortPlayerStartCreative`** Z in that level. **`EditorLevelLibrary.set_actor_label`** missing — use **`actor.set_actor_label`**. Editor **`line_trace_single`** short form returns empty **`HitResult`** here (no reliable surface snap). **`save_current_level`** OK.
- **UEFN MCP spawn (teleporters + vehicle):** Live **`spawn_actor`** placed **2×** Creative Teleporter (`BP_Creative_Device_Teleporter_C` → `/Game/Creative/Devices/Teleporter/BP_Creative_Device_Teleporter.BP_Creative_Device_Teleporter_C`) and **1×** vehicle spawner via **`Device_Floor_VehicleSpawner_C`** → `/Game/Items/Traps/Blueprints/Device_Floor_VehicleSpawner.Device_Floor_VehicleSpawner_C` (Creative BP paths under `/Game/Creative/Devices/VehicleSpawner/BP_*` did **not** `load_class` on that engine). Level **`save_current_level`** succeeded after placement.
- **Creative Teleporter kits:** Catalog blueprint **`BP_Creative_Device_Teleporter_C`** → `/Game/Creative/Devices/Teleporter/BP_Creative_Device_Teleporter.BP_Creative_Device_Teleporter_C` (live `spawn_actor` OK). **`entity_kits.py`:** new kit **`Teleport`** (single pad); **`Teleport Link`** now uses same blueprint for both pads; **`CLASS_FALLBACKS`** maps BP → `/Script/FortniteGame.FortCreativeTeleporter`.
- **Creative Button via MCP:** Engine `/Script/FortniteGame.*` names did not `load_class`; `device_catalog_scan` + Saved `device_catalog.json` surfaced **`BP_Creative_Button_C`** at `/Game/Creative/Devices/Button/BP_Creative_Button.BP_Creative_Button_C`. Live **`spawn_actor`** with that path succeeded (actor label `Button`). Repo: **`entity_kits.py`** kit **`Button`** added for `entity_spawn_kit` after deploy.
- **FEATURES.md audit via user-uefn MCP:** `ping` OK (bridge `UEFN uefn_tools MCP Bridge`, Python 3.11.8, port **8765**). `get_status`: `running=True`, **38** bridge commands. `list_tools` payload parsed: **354** registry tool names **identical** to FEATURES.md `run_tool` table (set diff empty). FEATURES.md native table remains **31** tools (matches `scripts/gen_features_md.py` `NATIVE_MCP`). Status columns in FEATURES.md are still **Untested** for behavioral verification; this pass only validated inventory parity + live connectivity.

